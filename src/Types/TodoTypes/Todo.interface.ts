export interface ISmeta {
       id: number | string;
       title: string;
       author: string;
       body: string;
}
export interface IAddSmeta {
       title: string;
       body: string;
       author: string;
}